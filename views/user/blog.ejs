<%- include("navbar.ejs") %>

<style>
/* Articles Section */
.articles-section {
    padding: 50px 20px;
    text-align: center;
}

.articles-section h2 {
    font-size:34px;
    color: #1b1b6d;
    
    
   
}

.article-card {
    background: white;
    width: 100%;
    max-width: 350px;
    border-radius: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    overflow: hidden;
    transition: transform 0.3s ease-in-out;
    position: relative;
    border-bottom-right-radius: 60px;
    border-top-left-radius: 60px;
    margin-top: 30px;
}

.article-card:hover {
    transform: scale(1.05);
}

.article-img {
    width: 100%;
    height: 200px;
    object-fit: cover;
}

.article-content {
    padding: 20px;
}

.article-category {
    font-size: 0.9rem;
    font-weight: bold;
    color: #ff4081;
    text-transform: uppercase;
}

.article-date {
    font-size: 0.8rem;
    color: #666;
    margin: 5px 0;
}

.article-title {
    font-size: 1.2rem;
    font-weight: bold;
    color: #1b1b6d;
    margin-bottom: 10px;
}

.read-more {
    display: inline-block;
    padding: 8px 12px;
    background-color: #1b1b6d;
    color: white;
    font-size: 0.9rem;
    text-decoration: none;
    border-radius: 5px;
    transition: 0.3s;
}

.read-more:hover {
    background-color: #ff4081;
}
</style>

<!-- 🟢 Articles Section with AOS Animations -->
<section class="articles-section py-5">
    <div class="container">
        <h2 class="text-center mb-4" data-aos="fade-up">Latest Blogs & Articles</h2>
        <div class="row justify-content-center">
            <% articles.forEach((article, index) => { %>
                <div class="col-md-4 d-flex justify-content-center mb-4" data-aos="fade-up" data-aos-delay="<%= index * 150 %>">
                    <div class="article-card shadow">
                        <img src="/admin_assets/<%= article.image %>" alt="<%= article.title %>" class="article-img">
                        <div class="article-content">
                            <p class="article-category"><%= article.category %></p>
                            <p class="article-date"><%= article.date %></p>
                            <h3 class="article-title"><%= article.title %></h3>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
       
    </div>
</section>

<script>
    AOS.init({
        duration: 800,
        easing: "ease-in-out",
        once: true
    });
</script>



<style>
/* Blog Section */
.blog-section {
    padding: 50px 20px;
    text-align: center;
}

.blog-section h2 {
    font-size: 2.5rem;
    color: #1b1b6d;
    margin-bottom: 30px;
    font-weight: bold;
}

/* Video Section */
.video-container {
    position: relative;
    width: 100%;
    max-width: 600px;
    margin: auto;
}

.video-container iframe {
    width: 100%;
    height: 315px;
    border-radius: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}

/* Info Section */
.info-section {
    background: #f8f9fa;
    padding: 20px;
    border-radius: 10px;
    text-align: left;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

.info-section p {
    font-size: 1rem;
    color: #333;
    line-height: 1.6;
}

/* Blog Cards */
.blog-card {
    background: white;
    border-radius: 10px;
    overflow: hidden;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s;
    text-align: left;
}

.blog-card:hover {
    transform: scale(1.05);
}

.blog-img {
    width: 100%;
    height: 200px;
    object-fit: cover;
}

.blog-content {
    padding: 15px;
}

.blog-title {
    font-size: 1.2rem;
    font-weight: bold;
    color: #1b1b6d;
    margin-bottom: 10px;
}

.read-more {
    display: inline-block;
    padding: 8px 12px;
    background-color: #1b1b6d;
    color: white;
    font-size: 0.9rem;
    text-decoration: none;
    border-radius: 5px;
    transition: 0.3s;
}

.read-more:hover {
    background-color: #ff4081;
}
</style>


            <!-- Right Column: Information -->
            <!-- <div class="col-md-6">
                <div class="info-section">
                    <p style="font-size: 1.1rem;">
                        At Star Kidz Hospital, our experts bring the best healthcare services with advanced medical treatments.
                        Our doctors are highly skilled in various specialties, ensuring the highest quality care for every patient.
                        Stay updated with the latest health tips, medical breakthroughs, and hospital updates through our blogs.
                    </p>
                </div>
            </div>
        </div> -->

        <!-- ✅ Blog Section -->
<section class="blog-section py-5">
    <div class="container">
        <!-- <h2 class="text-center mb-4" data-aos="fade-up">Latest Blog Posts & Articles</h2> -->

        <% blogs.forEach((blog, index) => { %>
            <div class="row mt-5 align-items-center" data-aos="fade-up">
                <!-- Left Column: Video -->
                <div class="col-md-6 text-center">
                    <div class="video-container">
                        <h3 style="color: darkblue; font-weight: bold;">Watch Our Experts in Action</h3>
                        <iframe width="100%" height="315" src="<%= blog.video_url ? blog.video_url : 'https://www.youtube.com/embed/sItEKkt8vJ4' %>" 
                            title="Specialist Video" frameborder="0" 
                            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
                            allowfullscreen>
                        </iframe>
                    </div>
                </div>

                <!-- Right Column: Information -->
                <div class="col-md-6">
                    <div class="info-section">
                        <h3 class="blog-title"><%= blog.title %></h3>
                        <p class="text-muted">
                            <strong><%= blog.author %></strong> | 
                            <%= new Date(blog.created_at).toDateString() %>
                        </p>
                        <p style="font-size: 1.1rem;"><%= blog.content.substring(0, 150) %>...</p>
                        <!-- <a href="/blogs/<%= blogs.id %>" class="btn btn-primary btn-sm">Read More</a> -->
                    </div>
                </div>
            </div>
        <% }); %>

        <!-- View All Button -->
        <!-- <div class="text-center mt-5">
            <a href="/blogs" class="btn btn-darkblue">View All Articles</a>
        </div> -->
    </div>
</section>

<!-- AOS Animation -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.js"></script>
<script>
    AOS.init({
        duration: 800,
        easing: "ease-in-out",
        once: true
    });
</script>

<%- include("footer.ejs") %>

