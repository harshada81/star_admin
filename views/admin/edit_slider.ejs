<%- include("navbar.ejs") %>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Slider</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>

    <div class="container mt-5">
        <h2 class="text-center text-primary">Edit Slider</h2>

        <form action="/admin/update_slider" method="POST" enctype="multipart/form-data">
            <input type="hidden" name="slider_id" value="<%= slider.slider_id %>">
            
            <!-- Text Inputs -->
            <div class="mb-3">
                <label class="form-label">Text 1:</label>
                <input type="text" name="text1" class="form-control" value="<%= slider.text1 %>" required>
            </div>
            <div class="mb-3">
                <label class="form-label">Text 2:</label>
                <input type="text" name="text2" class="form-control" value="<%= slider.text2 %>" required>
            </div>
            <div class="mb-3">
                <label class="form-label">Text 3:</label>
                <input type="text" name="text3" class="form-control" value="<%= slider.text3 %>" required>
            </div>

            <!-- Image Uploads -->
            <div class="mb-3">
                <label class="form-label">Image 1:</label>
                <input type="file" name="img1" class="form-control">
                <% if (slider.img1) { %>
                    <img src="/admin_assets/<%= slider.img1 %>" alt="Slider Image 1" class="mt-2" width="150">
                    <input type="show" name="existing_img1" value="<%= slider.img1 %>">
                <% } %>
            </div>

            <div class="mb-3">
                <label class="form-label">Image 2:</label>
                <input type="file" name="img2" class="form-control">
                <% if (slider.img2) { %>
                    <img src="/admin_assets/<%= slider.img2 %>" alt="Slider Image 2" class="mt-2" width="150">
                    <input type="show" name="existing_img2" value="<%= slider.img2 %>">
                <% } %>
            </div>

            <div class="mb-3">
                <label class="form-label">Image 3:</label>
                <input type="file" name="img3" class="form-control">
                <% if (slider.img3) { %>
                    <img src="/admin_assets/<%= slider.img3 %>" alt="Slider Image 3" class="mt-2" width="150">
                    <input type="show" name="existing_img3" value="<%= slider.img3 %>">
                <% } %>
            </div>

            <button type="submit" class="btn btn-success">Update Slider</button>
            <a href="/admin/slider" class="btn btn-secondary">Cancel</a>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

<%- include("footer.ejs") %>
