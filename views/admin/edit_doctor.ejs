<%- include("navbar.ejs") %>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Doctor</title>
    <link rel="stylesheet" href="/admin_assets/css/bootstrap.min.css">
</head>
<body>

<div class="container mt-5">
    <h2 class="text-center mb-4">Edit Doctor</h2>

    <!-- 📌 Edit Doctor Form -->
    <form action="/admin/update_doctor/<%= doctor.id %>" method="POST" enctype="multipart/form-data" class="p-4 border rounded">
        <input type="hidden" name="old_image" value="<%= doctor.image %>">

        <div class="mb-3">
            <label class="form-label">Doctor Name</label>
            <input type="text" class="form-control" name="name" value="<%= doctor.name %>" required>
        </div>

        <div class="mb-3">
            <label class="form-label">Specialization</label>
            <input type="text" class="form-control" name="specialization" value="<%= doctor.specialization %>" required>
        </div>

        <div class="mb-3">
            <label class="form-label">Description</label>
            <textarea class="form-control" name="description" rows="4" required><%= doctor.description %></textarea>
        </div>

        <div class="mb-3">
            <label class="form-label">Current Image</label>
            <br>
            <img src="/admin_assets/<%= doctor.image %>" alt="Doctor Image" width="150" class="border rounded">
        </div>

        <div class="mb-3">
            <label class="form-label">Upload New Image</label>
            <input type="file" class="form-control" name="image" accept="image/*">
        </div>

        <button type="submit" class="btn btn-success">Update Doctor</button>
        <a href="/admin/doctors" class="btn btn-secondary">Cancel</a>
    </form>
</div>

</body>
</html>
<%- include("footer.ejs") %>
