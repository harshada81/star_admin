<%- include("navbar.ejs") %>

<div class="container mt-4">
    <h2 class="text-center text-primary">Our Achievements & Awards</h2>
    <p class="text-center">Star Kidz Hospital has been recognized for excellence in pediatric care. Our commitment to children’s health has earned us numerous awards for outstanding service, patient satisfaction, and medical innovation.</p>
    <div class="row">
        <% if (achievements.length > 0) { %>
            <% achievements.forEach((award) => { %>
                <div class="col-md-4 text-center mb-4">
                    <div class="card p-3 shadow-sm">
                        <img src="<%- award.image %>" class="card-img-top" alt="<%- award.title %>" style="width: 100%; height: 200px; object-fit: cover;">
                        <div class="card-body">
                            <h5 class="card-title"><%- award.title %></h5>
                            <p class="text-muted">🏆 <%- award.year %></p>
                        </div>
                    </div>
                </div>
            <% }); %>
        <% } else { %>
            <p class="text-center">No achievements available</p>
        <% } %>
    </div>
</div>

<%- include("footer.ejs") %>
